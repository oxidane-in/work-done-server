{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "ProjectOtherCostPlannedUpdateRequest",
  "description": "Update request for Project Other Cost Planned",
  "properties": {
    "projectId": {
      "type": ["integer", "null"],
      "minimum": 1,
      "description": "Reference to the project ID"
    },
    "otherCostItemId": {
      "type": ["integer", "null"],
      "minimum": 1,
      "description": "Reference to the other cost item ID"
    },
    "projectOtherCostPlannedAmount": {
      "type": ["number", "null"],
      "minimum": 0,
      "maximum": 999999999999.99,
      "description": "Planned amount for the other cost"
    },
    "projectOtherCostPlannedRemark": {
      "type": ["string", "null"],
      "description": "Remarks for the planned other cost"
    }
  },
  "additionalProperties": false
}
